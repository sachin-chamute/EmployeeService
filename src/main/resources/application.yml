server:
  port: 9087

spring:
  datasource:
     url: jdbc:postgresql://localhost:5432/emp_db
     username: postgres
     password: admin100
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate: 
          format_sql: true
          dialect: org.hibernate.dialect.PostgreSQL81Dialect
